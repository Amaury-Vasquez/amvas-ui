import { FC } from 'react';
import { Slider } from '@/components';

const SliderOverview: FC = () => (
  <div className="w-full h-full items-center justify-center flex p-4">
    <Slider className="w-full" />
  </div>
);

export default SliderOverview;
